server:
  applicationConnectors:
  - type: http
    bindHost: 127.0.0.1
  adminConnectors:
  - type: http
    bindHost: 127.0.0.1
logging:
  level: INFO
  loggers:
    org.metadatacenter: DEBUG
  appenders:
    - type: console
      threshold: ALL
      timeZone: UTC
      target: stdout
